get:
  tags:
    - "authentication"
  summary: "return access_token"
  security: []
  responses:
    "200":
      description: "login"
      schema:
        properties:
          access_token:
            type: "string"
            format: "string"
    default:
      description: "error"
      schema:
        $ref: "#/definitions/error"